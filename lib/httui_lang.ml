(* Public entry point for the httui language layer.
   Modules will be added in subsequent work — parsing, semantics, type
   inference, completion, diagnostics, codegen, and formatter. *)

let version = "0.1.0"
